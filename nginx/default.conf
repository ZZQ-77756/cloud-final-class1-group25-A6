server {
    listen 80;
    server_name localhost;

    # 前端静态页面
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # 后端 API 转发 (反向代理核心配置)
    location /api/ {
        proxy_pass http://backend:5000; # 'backend' 是 docker-compose 里的服务名
    }
}
